function mk_alias() { 
    name=$1
    shift
    for f in "$@" ; do
        first_word=`echo $f | cut -d ' ' -f1`
        if type $first_word >/dev/null 2>&1 ; then 
            alias $name="`echo $f`"
            break
        fi
    done
}
