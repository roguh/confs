# get_config().InteractiveShell.colors = "Linux"
get_config().InteractiveShell.colors = "lightbg"
get_config().TerminalInteractiveShell.term_title_format = "IPython REPL: {cwd}"
# get_config().TerminalInteractiveShell.mouse_support = True

get_config().InteractiveShell.history_length = int(1e6)
